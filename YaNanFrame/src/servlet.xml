<?xml version="1.0" encoding="UTF-8"?>
<!-- 上传文件配置暂不支持 -->
<servlet-mapping>
	<!-- 该配置用于区分不同命名空间的action，action的后缀统一为 ****.do 
	package 中的 namespace action所属父命名空间，如果为 
		* 则对所有同名action有效 否则仅对该命名空间的action有效，
		extends 目前只支持servlet-default servlet 
		name action的名称，如login 对应action为*/login.do 
		class 为处理的java类，该类如果需要使用高级功能， 例如访问session，request、response 
		context需要继承自DefaultServlet，否则只能不能获取，但依然可以使用 token，访问数据库，表单理等功能。
		 method java类中处理的方法，倘若方法不存在，默认使用execute，返回true之 
		<result> 该标签可以不使用，如果不用的话，调配器会默认返回java类返回的值 
		name 返回值 标签里为重定向的地址 -->
	<package package="com.UFO.action"></package>	
</servlet-mapping>